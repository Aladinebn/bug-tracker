pipeline {
    agent any

    stages {
        stage('hello') {
            steps {
                echo 'Hello World'
              sh 'echo "hello jenkins"'
                sh 'date'
            }
        }
    }
}