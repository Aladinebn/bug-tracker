pipeline {
    agent any

    stages {
        stage('hello') {
            steps {
                echo 'Hello World'
                sh 'ech"hello jenkins"'
                sh 'date'
            }
        }
    }
}