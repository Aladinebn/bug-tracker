pipeline {

    // Exécute le pipeline sur n'importe quel agent Jenkins disponible
    agent any

    stages {

        // Exécute les tests Backend et Frontend en parallèle
        stage('Running Unit Tests') {
            parallel {

                // ============================
                // BACKEND UNIT TESTS
                // ============================
                stage('Unit Test - Backend') {

                    agent {
                        docker {
                            image 'snakee/golang-junit:1.21'
                            reuseNode true
                        }
                    }

                    environment {
                        HOME = "${WORKSPACE}"
                        GOCACHE = "${WORKSPACE}/.cache/go-build"
                        GOPATH = "${WORKSPACE}/.cache/go-path"
                    }

                    steps {
                        dir('bugtracker-backend') {

                            sh '''
                                # Création des dossiers nécessaires
                                mkdir -p "$GOCACHE"
                                mkdir -p "$GOPATH"
                                mkdir -p reports
                                mkdir -p test-results

                                # Exécution des tests et génération du rapport JUnit
                                go test -v ./... 2>&1 | go-junit-report > test-results/test-results.xml

                                # Génération du rapport de couverture
                                go test -coverprofile=coverage.out -covermode=atomic ./...

                                # Génération du rapport HTML
                                go tool cover -html=coverage.out -o reports/coverage.html
                            '''
                        }
                    }

                    post {
                        always {
                            junit allowEmptyResults: true,
                                  testResults: 'bugtracker-backend/test-results.xml'

                            publishHTML(target: [
                                allowMissing: true,
                                alwaysLinkToLastBuild: true,
                                keepAll: true,
                                reportDir: 'bugtracker-backend/reports',
                                reportFiles: 'coverage.html',
                                reportName: 'Backend Coverage Report'
                            ])
                        }
                    }
                }

                // ============================
                // FRONTEND UNIT TESTS
                // ============================
                stage('Unit Test - Frontend') {

                    agent {
                        docker {
                            image 'node:20-alpine'
                            reuseNode true
                        }
                    }

                    steps {
                        dir('bugtracker-frontend') {

                            sh '''
                                # Installation des dépendances
                                npm ci

                                # Exécution des tests
                                npm test
                            '''
                        }
                    }

                    post {
                        always {

                            junit allowEmptyResults: false,
                                  testResults: 'bugtracker-frontend/test-results.xml'

                            publishHTML(target: [
                                allowMissing: false,
                                alwaysLinkToLastBuild: true,
                                keepAll: true,
                                reportDir: 'bugtracker-frontend/coverage',
                                reportFiles: 'index.html',
                                reportName: 'Frontend Coverage Report'
                            ])
                        }
                    }
                }
             stage('Launch Application') {
                agent {
                    docker {
                        image 'docker:20.10.24-dind'
                        reuseNode true
                        // Montre le socket Docker pour permettre l'exécution de Docker à l'intérieur du conteneur
                        args '-v /var/run/docker.sock:/var/run/docker.sock' 
                    }
                }
                steps {
                    sh '''
                        # Lancer l'application en arrière-plan
                        docker compose up --build -d 
                    '''
                }
             }
            }           
        }
    }

    // ============================
    // FIN DU PIPELINE
    // ============================
    post {

        always {
            cleanWs()
            echo 'Pipeline terminé.'
        }

        success {
            echo 'Tous les tests sont passés avec succès.'
        }

        failure {
            echo 'Le pipeline a échoué.'
        }
    }
}